apiVersion: v1
kind: Service
metadata:
  name: domains-llm-gateway
  namespace: lab-domains-sre
spec:
  type: NodePort
  selector:
    app: litellm-gateway
  ports:
    - name: openai
      port: 4000
      targetPort: 4000
      nodePort: 31440
      protocol: TCP
