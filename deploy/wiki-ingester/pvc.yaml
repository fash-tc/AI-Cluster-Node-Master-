apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wiki-ingester-pydeps
  namespace: lab-domains-sre
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
  storageClassName: local-path
